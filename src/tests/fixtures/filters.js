import moment from 'moment';

const filters = {
    text: '',
    sortBy: 'date',
    startDate: undefined,
    endDate: undefined
};

const altFilters = {
    text: '',
    sortBy: 'amount',
    startDate: undefined,
    endDate: undefined
};

export { filters, altFilters };